[package]
name = "mycoolapp"
version = "1.0.0"
description = "React Vite app compiled offline into native Windows, Linux & Android executables"
authors = ["Developer"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
tauri = { version = "2.0.0", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
